#include <bits/stdc++.h>
using namespace std;
int main ()
{
    string s,k;
    int c=0;
    cin>>s;
    for (int i=0; s[i] != '\0'; i++)
    {
        if (s[i] = s[i]+1)
        {
           k= char(c++);
        }
    }
    cout<<k<<endl;

    return 0;
}
